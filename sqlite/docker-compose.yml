services:
  sqlite:
    image: keinos/sqlite3:latest
    container_name: uni-sqlite
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: ["sqlite3", "/workspace/university.sqlite", ".read /workspace/init/01_books.sql"]
