services:
  neo4j:
    image: neo4j:5
    container_name: uni-neo4j
    environment:
      NEO4J_AUTH: neo4j/uni_password
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j-data:/data

volumes:
  neo4j-data:
